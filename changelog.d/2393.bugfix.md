`pip-compile --rebuild` now clears its package download cache by moving the
cache directory aside before deleting it, avoiding partially-deleted cache trees
during parallel invocations -- by {user}`Sean-Kenneth-Doherty`.
