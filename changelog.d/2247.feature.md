The ``pip-compile`` header now includes the ``pip-tools`` version alongside the
Python version, making it easier to reproduce a compiled output file with the
same tooling -- by {user}`shivamtiwari3`.
