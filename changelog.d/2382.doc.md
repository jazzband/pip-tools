Restructured the documentation site under [Diátaxis](https://diataxis.fr/) and slimmed
the `README.md` from 750 lines to a one-paragraph summary plus quick examples
that link out to the docs -- by {user}`gaborbernat`.

The new tree splits content into a tutorial, focused how-to recipes, an
authoritative CLI reference, and an explanation section that covers the
resolver model, caching layers, the local-pin proxy, build-system integration,
upcoming default flips, and a comparison with `uv`, `poetry`, and `pdm`.
Mermaid diagrams replace prose where graphs help; tabs cover parallel
platforms; admonitions (`warning`, `deprecated`, `versionchanged`, `seealso`)
replace inline `WARNING:` blocks. The CLI reference is generated with
`sphinx-click` against the actual `click` commands so flags stay in sync with
the source.
