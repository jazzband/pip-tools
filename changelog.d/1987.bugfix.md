Resolved requirement lines written to an ``--output-file`` are no longer
mirrored to stderr at default verbosity. Pass ``-v``/``--verbose`` (or higher)
to echo them while compiling. Dry-run output is unchanged.
-- by {user}`MohammedAnasNathani`
